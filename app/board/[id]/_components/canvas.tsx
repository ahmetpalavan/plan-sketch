import React from 'react';
import { Info } from './info';

export const Canvas = () => {
  return (
    <main className='w-full h-full relative bg-yellow-100 touch-none'>
      <Info />
    </main>
  );
};
